async function pix(client, numero) {

    await client.sendText(
        numero,

`💳 PIX

Chave PIX:

financeiro@toptecdigital.com

Favorecido:

Thiarles R Dias

Após o pagamento envie o comprovante.

0️⃣ Voltar`
    );

}

module.exports = pix;